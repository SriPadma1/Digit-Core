name: Build Pipeline
on:
  workflow_dispatch:
  push:
    branches:
      - mdms-v2
jobs:
  list-branches:
    runs-on: ubuntu-latest
    steps:
      - name: Displaying the branch name
        run: echo ${{ github.ref_name }}